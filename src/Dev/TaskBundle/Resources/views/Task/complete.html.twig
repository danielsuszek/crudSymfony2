{% extends '::base.html.twig' %}
{% form_theme edit_form 'DevTaskBundle:Form:fields.html.twig'%}
{% block body -%}
    <h1>Task complete</h1>
{#
    {# {{ form(edit_form) }} #}
    {# {{ form_start(edit_form) }} #}    
    {{ form_start(edit_form, {'action': path('task_update_complete', { 'id': entity.id })} ) }}
    
    
    <div>
        {# {{ form_widget(form.commentContent) }} #}
        
        {# {{ form_errors(edit_form.task) }} #}
        
        {{ form_errors(edit_form) }}
        {{ form_widget(edit_form.complete) }}
        {# {{ form_label(edit_form.commentContent) }} #}
        <p>
            <button type="submit">Edit</button>
        </p>
    </div>
    {{ form_end(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('task') }}">
            Back to the list
        </a>
    </li>

</ul>
        
{% endblock %}
